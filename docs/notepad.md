# Notepad

A place to keep some notes and used generator commands.

## Event Resource

Make a basic resource for events using the standard generators.

```bash
$ mix phx.gen.html Calendar Event events title:string starts_at:utc_datetime ends_at:utc_datetime --binary-id
```
